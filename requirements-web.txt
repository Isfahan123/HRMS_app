# Web-only dependencies for cPanel/Exabytes deployment
# This file is now functionally identical to requirements.txt
# Both files are web-compatible since desktop dependencies were moved to requirements-desktop.txt
#
# PYTHON VERSION REQUIREMENT: Python >= 3.9 (Python 3.11 recommended)
# Several packages require Python 3.9+:
#   - supabase>=2.8.1 requires Python >=3.9
#   - holidays>=0.81 requires Python >=3.9
#   - pandas>=2.2.2 requires Python >=3.9
#
# Installation: pip install -r requirements-web.txt
# Or simply: pip install -r requirements.txt (now also web-safe)

# Database
supabase==2.8.1

# Authentication & Security
bcrypt==4.2.0

# Utilities
pytz==2024.1
python-dotenv==1.0.1
requests==2.32.3
num2words==0.5.13
holidays==0.81

# Data processing (required by services)
# - reportlab: PDF payslip generation
# - pandas: Data analysis and Excel file parsing
# - openpyxl: Excel backend for pandas
# - PyPDF2: PDF processing
reportlab==4.2.2
pandas==2.2.2
openpyxl==3.1.2
PyPDF2==3.0.1

# FastAPI stack (Web Framework)
fastapi==0.115.5
uvicorn==0.32.1
jinja2==3.1.4
python-multipart==0.0.18

# REQUIRED for FastAPI to run under cPanel Passenger
# a2wsgi converts ASGI (FastAPI) to WSGI (Passenger)
a2wsgi==1.10.10

# Note: Desktop-only packages are in requirements-desktop.txt:
# - PyQt5 (Desktop GUI - causes errors on cPanel)
# - tabula-py (Requires Java - causes errors on cPanel)
