# HRMS Web Application Minimal Dependencies
# Use this if full requirements.txt fails to install
# Python >= 3.8 required
#
# Install via cPanel Terminal or SSH:
#   pip install -r requirements-minimal.txt

# Core dependencies only - NO PDF generation
passlib>=1.7.4
pytz>=2024.1
python-dotenv>=1.0.0
requests>=2.31.0
num2words>=0.5.12

# Web framework
flask>=3.0.0
jinja2>=3.1.0
