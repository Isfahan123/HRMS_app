# HRMS Web Application - Environment Configuration
# Copy this file to .env and fill in your actual values

# Supabase Configuration (Required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key-here

# Flask Configuration (Required)
# Generate a secret key using: python -c "import os; print(os.urandom(24).hex())"
FLASK_SECRET_KEY=your-random-secret-key-here

# Flask Environment (Optional)
# Set to 'development' for debugging, 'production' for deployment
FLASK_ENV=development

# Flask Debug Mode (Optional)
# Set to 1 for development, 0 for production
FLASK_DEBUG=1

# Flask Port (Optional)
# Default is 5000
FLASK_PORT=5000

# Application Settings (Optional)
# APP_NAME=HRMS
# APP_VERSION=1.0.0

# Database Pool Settings (Optional)
# DB_POOL_SIZE=10
# DB_POOL_TIMEOUT=30

# Session Settings (Optional)
# SESSION_LIFETIME=3600
# SESSION_COOKIE_SECURE=False  # Set to True in production with HTTPS
