from PyQt5.QtWidgets import QWidget, QVBoxLayout, QLabel, QPushButton, QLineEdit, QTextEdit, QListWidget, QFormLayout, QMessageBox, QTabWidget

# Training/Course Record Tab (Employee)
class EmployeeTrainingCourseTab(QWidget):
    def __init__(self, parent=None):
        super().__init__(parent)
        layout = QVBoxLayout()
        self.tab_widget = QTabWidget()

        # Submit subtab
        submit_tab = QWidget()




# Overseas Work/Trip Record Tab (Employee)
class EmployeeOverseasWorkTripTab(QWidget):
    def __init__(self, parent=None):
        super().__init__(parent)
        layout = QVBoxLayout()
        self.tab_widget = QTabWidget()

        # Submit subtab
        submit_tab = QWidget()



